import{AppError}from"../../shared/errors/app-error.js";import{DealRepository}from"./deal.repository.js";import type{DealInput}from"./deal.validation.js";export class DealService{constructor(private r=new DealRepository()){}async list(o:string,a:boolean){const deals=await this.r.list(o,a);const open=deals.filter(d=>!["WON","LOST"].includes(d.stage));const won=deals.filter(d=>d.stage==="WON");return{deals,metrics:{openDeals:open.length,pipelineValue:open.reduce((s,d)=>s+Number(d.amount),0),weightedValue:open.reduce((s,d)=>s+Number(d.amount)*d.probability/100,0),wonRevenue:won.reduce((s,d)=>s+Number(d.amount),0),wonDeals:won.length}}}async customer(o:string,id:string){if(!await this.r.customer(o,id))throw new AppError(404,"Customer was not found.","CUSTOMER_NOT_FOUND")}async create(o:string,u:string,i:DealInput){await this.customer(o,i.customerId);return this.r.create(o,u,i)}async update(o:string,u:string,id:string,i:DealInput){await this.customer(o,i.customerId);if((await this.r.update(o,id,u,i)).count!==1)throw new AppError(404,"Deal was not found.","DEAL_NOT_FOUND");return this.r.find(o,id)}async archive(o:string,u:string,id:string){if((await this.r.archive(o,id,u)).count!==1)throw new AppError(404,"Deal was not found.","DEAL_NOT_FOUND")}async restore(o:string,u:string,id:string){if((await this.r.restore(o,id,u)).count!==1)throw new AppError(404,"Archived deal was not found.","DEAL_NOT_FOUND")}}

import{describe,expect,it}from"vitest";import{deploymentSchema,requestSchema,websiteSchema}from"../src/modules/websites/website.validation.js";const website={customerId:null,assignedEmployeeId:null,name:"Main site",domain:"https://example.com/",platform:"CUSTOM",status:"ACTIVE",adminUrl:"",repositoryUrl:"",hostingProvider:"Vercel",notes:""};describe("website validation",()=>{it("normalizes domains without storing secrets",()=>expect(websiteSchema.parse(website)).toMatchObject({domain:"example.com",adminUrl:null}));it("rejects tenant and audit identifiers",()=>expect(()=>websiteSchema.parse({...website,organizationId:crypto.randomUUID(),createdById:crypto.randomUUID()})).toThrow());it("requires rollback planning for production",()=>expect(()=>deploymentSchema.parse({requestId:crypto.randomUUID(),environment:"PRODUCTION",status:"PLANNED",version:"",deploymentUrl:"",summary:"Banner release",verification:"",rollbackPlan:"",failureReason:""})).toThrow());it("does not accept approval identity from change requests",()=>expect(()=>requestSchema.parse({websiteId:crypto.randomUUID(),projectId:null,title:"Change banner",description:"Replace the homepage banner",type:"BANNER",priority:"MEDIUM",risk:"LOW",status:"AWAITING_APPROVAL",deadline:null,approvedById:crypto.randomUUID()})).toThrow())});

import{describe,expect,it}from"vitest";import{conversionSchema,inquirySchema}from"../src/modules/inquiries/inquiry.validation.js";const inquiry={source:"WHATSAPP",type:"UNCLASSIFIED",status:"NEW",priority:"MEDIUM",contactName:"A real customer",email:"customer@example.com",phone:"",companyName:"",subject:"Product question",message:"Please share the available sizes.",campaignId:null,assignedEmployeeId:null,responseDueAt:null,disqualifiedReason:""};describe("inquiry validation",()=>{it("normalizes optional contact data",()=>expect(inquirySchema.parse(inquiry)).toMatchObject({phone:null,companyName:null,disqualifiedReason:null}));it("rejects trusted tenant and creator identifiers",()=>expect(()=>inquirySchema.parse({...inquiry,organizationId:crypto.randomUUID(),createdById:crypto.randomUUID()})).toThrow());it("requires email or phone",()=>expect(()=>inquirySchema.parse({...inquiry,email:"",phone:""})).toThrow());it("only accepts target-specific conversion fields",()=>expect(()=>conversionSchema.parse({target:"CUSTOMER",organizationId:crypto.randomUUID()})).toThrow())});

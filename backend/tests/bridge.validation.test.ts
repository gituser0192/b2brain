import{describe,expect,it}from"vitest";import{connectorSchema,intakeSchema}from"../src/modules/automation-bridge/bridge.validation.js";describe("automation bridge validation",()=>{it("rejects trusted tenant identifiers",()=>expect(()=>connectorSchema.parse({name:"WhatsApp store",type:"WHATSAPP",provider:"Meta",externalAccountRef:"",status:"ACTIVE",mode:"MANUAL_APPROVAL",organizationId:crypto.randomUUID()})).toThrow());it("requires real contact details for communication events",()=>expect(()=>intakeSchema.parse({externalEventId:"wa-1",eventName:"message.received",kind:"INQUIRY",contactName:"",email:"",phone:"",subject:"",message:"",raw:{}})).toThrow());it("accepts traceable payment events without inventing customer data",()=>expect(intakeSchema.parse({externalEventId:"pay-1",eventName:"payment.received",kind:"PAYMENT",raw:{providerReference:"p-1"}})).toMatchObject({kind:"PAYMENT",contactName:null}))});

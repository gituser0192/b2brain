"use client";
import{useCallback,useEffect,useState}from"react";import{useAuth}from"@/features/auth/auth-context";import{ApiError}from"@/services/api-client";
interface Metrics{customers:number;leads:number;activeCustomers:number;overdueFollowUps:number;openDeals:number;pipelineValue:number;weightedForecast:number;wonRevenue:number;activeProjects:number;overdueTasks:number;activeEmployees:number;invoiced:number;received:number;outstanding:number;expenses:number;netCash:number;orders:number;activeOrders:number;orderValue:number;stockOnHand:number;stockReserved:number;lowStock:number;activeCampaigns:number;marketingSpend:number;marketingLeads:number;conversions:number;attributedRevenue:number;returnOnSpend:number;openTickets:number;overdueTickets:number}interface Alert{type:string;count:number;label:string;view:string}interface Payload{success:true;data:{periodDays:number|null;enabledServices:string[];currency:string;metrics:Metrics;alerts:Alert[]}}
const zero:Metrics={customers:0,leads:0,activeCustomers:0,overdueFollowUps:0,openDeals:0,pipelineValue:0,weightedForecast:0,wonRevenue:0,activeProjects:0,overdueTasks:0,activeEmployees:0,invoiced:0,received:0,outstanding:0,expenses:0,netCash:0,orders:0,activeOrders:0,orderValue:0,stockOnHand:0,stockReserved:0,lowStock:0,activeCampaigns:0,marketingSpend:0,marketingLeads:0,conversions:0,attributedRevenue:0,returnOnSpend:0,openTickets:0,overdueTickets:0};
export function BusinessDashboard({onNavigate}:{onNavigate:(view:string)=>void}){const{session,authorizedRequest}=useAuth();const[days,setDays]=useState("30");const[metrics,setMetrics]=useState(zero);const[services,setServices]=useState<string[]>([]);const[alerts,setAlerts]=useState<Alert[]>([]);const[error,setError]=useState("");const[loading,setLoading]=useState(true);const load=useCallback(async()=>{setLoading(true);try{const response=await authorizedRequest<Payload>(`/dashboard/summary?days=${days}`);setMetrics(response.data.metrics);setServices(response.data.enabledServices);setAlerts(response.data.alerts);setError("")}catch(reason){setError(reason instanceof ApiError?reason.message:"Unable to load dashboard.")}finally{setLoading(false)}},[authorizedRequest,days]);useEffect(()=>{const task=setTimeout(()=>void load(),0);return()=>clearTimeout(task)},[load]);const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:session?.organization.currency??"INR",maximumFractionDigits:0,notation:Math.abs(value)>=1000000?"compact":"standard"}).format(value);const has=(code:string)=>services.includes(code);const bars=[{label:"Pipeline",value:metrics.pipelineValue,color:"#6757e8"},{label:"Forecast",value:metrics.weightedForecast,color:"#2e9bea"},{label:"Won",value:metrics.wonRevenue,color:"#18a66a"}];const max=Math.max(1,...bars.map(item=>item.value));return <div className="business-dashboard"><header className="business-dashboard-head"><div><p>Unified business overview</p><h2>Good {new Date().getHours()<12?"morning":new Date().getHours()<18?"afternoon":"evening"}, {session?.user.firstName}.</h2><span>Every number below comes from your organization’s real service data.</span></div><label><span>Reporting period</span><select value={days} onChange={event=>setDays(event.target.value)}><option value="30">Last 30 days</option><option value="90">Last 90 days</option><option value="365">Last 12 months</option><option value="all">All time</option></select></label></header>{error&&<div className="dashboard-notice error">{error}</div>}{loading?<div className="dashboard-data-loader"><span className="spinner dark"/>Calculating your business…</div>:<><section className="executive-metrics">{has("FINANCE")&&<article className="primary"><div><span>Net cash</span><strong>{money(metrics.netCash)}</strong><small>{money(metrics.received)} received · {money(metrics.expenses)} expenses</small></div><i>₹</i></article>}{has("SALES")&&<article><span>Weighted forecast</span><strong>{money(metrics.weightedForecast)}</strong><small>{metrics.openDeals} open deals</small></article>}{has("ORDERS")&&<article><span>Order value</span><strong>{money(metrics.orderValue)}</strong><small>{metrics.activeOrders} requiring progress</small></article>}{has("MARKETING")&&<article><span>Marketing return</span><strong>{metrics.returnOnSpend.toFixed(2)}×</strong><small>{metrics.conversions} conversions</small></article>}{!services.some(code=>["FINANCE","SALES","ORDERS","MARKETING"].includes(code))&&<article className="primary"><div><span>Business metrics</span><strong>Ready when you are</strong><small>Assign business services to begin calculating.</small></div></article>}</section><section className="business-dashboard-grid"><div className="dashboard-panel performance-panel"><header><div><p>Revenue engine</p><h3>Sales performance</h3></div>{has("SALES")&&<button onClick={()=>onNavigate("sales")}>Open Sales →</button>}</header>{has("SALES")?<><div className="performance-bars">{bars.map(item=><div key={item.label}><span>{item.label}</span><div><i style={{width:`${item.value/max*100}%`,background:item.color}}/></div><strong>{money(item.value)}</strong></div>)}</div><div className="panel-stat-row"><span><small>Open deals</small><strong>{metrics.openDeals}</strong></span><span><small>Won revenue</small><strong>{money(metrics.wonRevenue)}</strong></span><span><small>Pipeline coverage</small><strong>{metrics.weightedForecast?`${(metrics.pipelineValue/metrics.weightedForecast).toFixed(1)}×`:"—"}</strong></span></div></>:<EmptyService text="Assign Sales to view pipeline performance."/>}</div><aside className="dashboard-panel attention-panel"><header><div><p>Needs attention</p><h3>Operational alerts</h3></div><span>{alerts.reduce((sum,item)=>sum+item.count,0)}</span></header>{alerts.length?<div>{alerts.map(item=><button key={item.type} onClick={()=>onNavigate(item.view)}><i>{item.count}</i><span><strong>{item.label}</strong><small>Review and take action</small></span><b>→</b></button>)}</div>:<div className="all-clear"><span>✓</span><strong>All clear</strong><p>No overdue or low-stock items were found.</p></div>}</aside><div className="dashboard-panel finance-panel"><header><div><p>Cash position</p><h3>Finance snapshot</h3></div>{has("FINANCE")&&<button onClick={()=>onNavigate("finance")}>Open Finance →</button>}</header>{has("FINANCE")?<div className="finance-snapshot"><div className="cash-ring" style={{"--paid":`${metrics.invoiced?Math.min(100,metrics.received/metrics.invoiced*100):0}%`} as React.CSSProperties}><span><strong>{metrics.invoiced?Math.round(metrics.received/metrics.invoiced*100):0}%</strong><small>collected</small></span></div><dl><div><dt>Invoiced</dt><dd>{money(metrics.invoiced)}</dd></div><div><dt>Received</dt><dd>{money(metrics.received)}</dd></div><div><dt>Outstanding</dt><dd>{money(metrics.outstanding)}</dd></div><div><dt>Expenses</dt><dd>{money(metrics.expenses)}</dd></div></dl></div>:<EmptyService text="Assign Finance to see cash and collection metrics."/>}</div><div className="dashboard-panel operations-panel"><header><div><p>Operating system</p><h3>Business activity</h3></div></header><div className="operations-cards">{has("CRM")&&<button onClick={()=>onNavigate("crm")}><i>C</i><span><strong>{metrics.customers}</strong><small>Customers · {metrics.leads} leads</small></span></button>}{has("PROJECTS")&&<button onClick={()=>onNavigate("projects")}><i>P</i><span><strong>{metrics.activeProjects}</strong><small>Active projects</small></span></button>}{has("PEOPLE")&&<button onClick={()=>onNavigate("employees")}><i>E</i><span><strong>{metrics.activeEmployees}</strong><small>Active employees</small></span></button>}{has("INVENTORY")&&<button onClick={()=>onNavigate("inventory")}><i>I</i><span><strong>{metrics.stockOnHand-metrics.stockReserved}</strong><small>Available stock units</small></span></button>}{has("MARKETING")&&<button onClick={()=>onNavigate("marketing")}><i>M</i><span><strong>{metrics.activeCampaigns}</strong><small>Active campaigns</small></span></button>}</div></div></section></>}</div>}
function EmptyService({text}:{text:string}){return <div className="panel-empty"><span>◇</span><p>{text}</p></div>}
